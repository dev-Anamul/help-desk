description: not found
content:
  application/json:
    schema:
      type: object
      properties:
        code:
          type: integer
          example: 404
        status:
          type: string
          example: "error"
        message:
          type: string
          example: "Not found"
