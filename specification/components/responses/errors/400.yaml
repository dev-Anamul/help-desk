description: bad request
content:
  application/json:
    schema:
      type: object
      properties:
        code:
          type: integer
          example: 400
        status:
          type: string
          example: 'error'
        message:
          type: string
          example: 'Bad request'
        errors:
          type: object
          properties:
            field:
              type: string
              example: message
