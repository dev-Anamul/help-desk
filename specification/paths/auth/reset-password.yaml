post:
  description: this api url is used to reset the login credentials if any one forgot the login credentials
  summary: reset password
  operationId: reset-password
  security: []
  tags:
    - Auth
  requestBody:
    $ref: ../../components/request-bodies/auth/reset-password.yaml
  responses:
    200:
      $ref: ../../components/responses/auth/reset-password.yaml
    400:
      $ref: ../../components/responses/errors/400.yaml
    404:
      $ref: ../../components/responses/errors/404.yaml
    500:
      $ref: ../../components/responses/errors/500.yaml
